# Server
PORT=3000

# Firebase Admin (server-side)
# Optional but recommended (improves initialization and multi-project setups)
FIREBASE_PROJECT_ID=

# Choose ONE of the following credential options:

# Option A (recommended for local dev): point to a service account JSON file
# Example (Windows): C:\\path\\to\\serviceAccountKey.json
GOOGLE_APPLICATION_CREDENTIALS=

# Option B: paste the full service account JSON as a single line string
# NOTE: If you paste multi-line JSON, wrap it and keep it valid JSON.
# Example:
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"...","private_key_id":"...","private_key":"-----BEGIN PRIVATE KEY-----\\n...\\n-----END PRIVATE KEY-----\\n","client_email":"...","client_id":"..."}
FIREBASE_SERVICE_ACCOUNT_JSON=
